import ContactoClient from './ContactoClient';

export const metadata = {
  title: 'Contacto — SrCloock',
  description: 'Escríbeme directamente desde este formulario.',
};

export default function Page() {
  return <ContactoClient />;
}
